{{- if and .Values.llm.apiKey (not .Values.llm.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "openhpa.name" . }}-llm
  labels:
    {{- include "openhpa.labels" . | nindent 4 }}
type: Opaque
stringData:
  apiKey: {{ .Values.llm.apiKey | quote }}
{{- end }}
